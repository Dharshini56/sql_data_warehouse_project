# sql_data_warehouse_project
An end-to-end data warehouse project using SQL Server and the medallion architecture. Raw ERP and CRM CSV data is ingested, cleaned, and modeled into a star schema (fact and dimension tables) to support analytics and reporting.
